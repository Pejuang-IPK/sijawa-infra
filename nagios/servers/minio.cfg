define host {
    use                     generic-host
    host_name               minio_server
    address                 sijawa-infra-minio-1
    contact_groups          admins
}

define service {
    use                     generic-service
    host_name               minio_server
    service_description     MinIO API Port 9000
    check_command           check_tcp!9000
}

define service {
    use                     generic-service
    host_name               minio_server
    service_description     MinIO Console Port 9001
    check_command           check_tcp!9001
}
