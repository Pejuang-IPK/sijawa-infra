define host {
    use                     generic-host
    host_name               mysql_server
    address                 mysql
}

define service {
    use                     generic-service
    host_name               mysql_server
    service_description     MySQL Service
    check_command           check_mysql!appuser!secret
}
