define host {
    use                     generic-host
    host_name               nginx_server
    address                 nginx_sijawa
    contact_groups          admins
}

define service {
    use                     generic-service
    host_name               nginx_server
    service_description     Nginx HTTP
    check_command           check_http
}

define service {
    use                     generic-service
    host_name               nginx_server
    service_description     Nginx Port 80
    check_command           check_tcp!80
}
